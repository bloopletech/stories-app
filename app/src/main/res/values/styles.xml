<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Hide the status bar -->
        <item name="android:windowFullscreen">true</item>
        <!-- Skip showing the application title -->
        <item name="android:windowNoTitle">true</item>
        <!--We will be using the toolbar so no need to show ActionBar-->
        <item name="android:windowActionBar">false</item>
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">#3F51B5</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryVariant">#303F9F</item>
        <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
        <item name="android:colorAccent">#FF4081</item>

        <item name="android:textAppearance">@style/AppTheme.TextAppearance</item>
        <item name="android:textAppearanceSmall">@style/AppTheme.TextAppearance</item>
    </style>

    <style name="AppTheme.TextAppearance" parent="@android:style/TextAppearance.Material">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">?android:textColorPrimary</item>
    </style>

    <style name="AppTheme.ActionBar" parent="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar">
    </style>
</resources>
